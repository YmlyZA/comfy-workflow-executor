# 手机版 UI/UX 适配 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `<md`（768px）断点下提供移动布局：底部 Tab 栏、Batches 卡片列表、触屏对比与滑动翻页、PWA 轻量外壳；`≥md` 桌面完全不变。

**Architecture:** 同一套组件 + Tailwind 响应式断点，无独立移动路由。JS 断点感知用新增 `useMediaQuery` hook（`useSyncExternalStore` + `matchMedia`，注入式设计使核心逻辑可在 node 环境单测）。DataTable 增加可选 `renderCard` prop，小屏用卡片渲染同一 TanStack Table 实例的行——筛选/排序/选择状态零重写。

**Tech Stack:** React 19、Tailwind v4、shadcn/ui（现有组件，零新依赖）、TanStack Table、sonner v2、Vitest（node 环境）。

**Spec:** `docs/superpowers/specs/2026-08-13-mobile-ui-design.md`

## Global Constraints

- 仅改前端（`apps/web`），服务端零改动。
- `≥md`（768px）桌面端零视觉改动；所有移动样式挂在默认断点、桌面样式挂 `md:` 前缀恢复现状。
- 不新增任何运行时/开发依赖；不改 `pnpm-workspace.yaml`。
- 断点口径统一：CSS 用 `md:`，JS 用 `useMediaQuery('(max-width: 767px)')`。
- web 包不写渲染测试；纯函数/store 逻辑写 Vitest 单测；其余靠 PR 手动验收清单。
- 每个 commit 结尾带 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
- 工作分支 `feat/mobile-ui`（已存在，spec 已提交）。

---

### Task 1: useMediaQuery hook

**Files:**
- Create: `apps/web/src/hooks/use-media-query.ts`
- Test: `apps/web/src/hooks/use-media-query.test.ts`

**Interfaces:**
- Produces: `useMediaQuery(query: string): boolean`（React hook，后续任务从 `@/hooks/use-media-query` 导入）；`matchMediaStore(query, host?)` 仅测试与 hook 内部用。

- [ ] **Step 1: 写失败测试**

`apps/web/src/hooks/use-media-query.test.ts`：

```ts
import { describe, expect, it } from 'vitest'
import { matchMediaStore, type MatchMediaHost } from './use-media-query'

/** 假 matchMedia 宿主:可手动翻转匹配态并触发 change 监听器 */
function fakeHost(initial: boolean) {
  let matches = initial
  const listeners = new Set<() => void>()
  const host: MatchMediaHost = {
    matchMedia: () => ({
      get matches() {
        return matches
      },
      addEventListener: (_type, cb) => listeners.add(cb),
      removeEventListener: (_type, cb) => listeners.delete(cb),
    }),
  }
  return {
    host,
    flip(v: boolean) {
      matches = v
      listeners.forEach((cb) => cb())
    },
    listenerCount: () => listeners.size,
  }
}

describe('matchMediaStore', () => {
  it('getSnapshot 返回当前匹配状态', () => {
    const { host } = fakeHost(true)
    expect(matchMediaStore('(max-width: 767px)', host).getSnapshot()).toBe(true)
  })

  it('匹配态变化时触发订阅回调,快照随之更新', () => {
    const { host, flip } = fakeHost(false)
    const store = matchMediaStore('(max-width: 767px)', host)
    let notified = 0
    store.subscribe(() => {
      notified += 1
    })
    flip(true)
    expect(notified).toBe(1)
    expect(store.getSnapshot()).toBe(true)
  })

  it('取消订阅后监听器被移除,不再收到通知', () => {
    const { host, flip, listenerCount } = fakeHost(false)
    const store = matchMediaStore('(max-width: 767px)', host)
    let notified = 0
    const unsubscribe = store.subscribe(() => {
      notified += 1
    })
    expect(listenerCount()).toBe(1)
    unsubscribe()
    expect(listenerCount()).toBe(0)
    flip(true)
    expect(notified).toBe(0)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @cwe/web test -- use-media-query`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现**

`apps/web/src/hooks/use-media-query.ts`：

```ts
import { useMemo, useSyncExternalStore } from 'react'

export interface MediaQueryLike {
  matches: boolean
  addEventListener(type: 'change', cb: () => void): void
  removeEventListener(type: 'change', cb: () => void): void
}

/** matchMedia 宿主抽象:生产用 window,测试注入假实现(vitest 跑在 node 无 DOM) */
export interface MatchMediaHost {
  matchMedia(query: string): MediaQueryLike
}

export function matchMediaStore(query: string, host: MatchMediaHost = window) {
  return {
    subscribe(onChange: () => void) {
      const mql = host.matchMedia(query)
      mql.addEventListener('change', onChange)
      return () => mql.removeEventListener('change', onChange)
    },
    getSnapshot() {
      return host.matchMedia(query).matches
    },
  }
}

/** 响应式断点判定,如 useMediaQuery('(max-width: 767px)') */
export function useMediaQuery(query: string): boolean {
  const store = useMemo(() => matchMediaStore(query), [query])
  return useSyncExternalStore(store.subscribe, store.getSnapshot)
}
```

- [ ] **Step 4: 跑测试确认通过 + typecheck**

Run: `pnpm --filter @cwe/web test -- use-media-query && pnpm --filter @cwe/web typecheck`
Expected: 3 个测试 PASS，typecheck 无错误

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/hooks/use-media-query.ts apps/web/src/hooks/use-media-query.test.ts
git commit -m "feat(web): useMediaQuery hook(useSyncExternalStore + 注入式 matchMedia)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: App 外壳——底部 Tab 栏 + 精简顶栏

**Files:**
- Create: `apps/web/src/components/mobile-tab-bar.tsx`
- Modify: `apps/web/src/App.tsx`（`RequireToken` 组件，约 37-66 行）

**Interfaces:**
- Consumes: 现有 `DropdownMenu` 系列、`cn`、react-router `NavLink`/`Link`/`useLocation`。
- Produces: `<MobileTabBar />`（无 props，自带 `md:hidden`）。

- [ ] **Step 1: 新建 MobileTabBar 组件**

`apps/web/src/components/mobile-tab-bar.tsx`：

```tsx
import { Link, NavLink, useLocation } from 'react-router-dom'
import { EllipsisIcon, ImagesIcon, LibraryIcon, WorkflowIcon } from 'lucide-react'
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from '@/components/ui/dropdown-menu'
import { cn } from '@/lib/utils'

const TABS = [
  { to: '/batches', label: 'Batches', Icon: ImagesIcon },
  { to: '/templates', label: 'Templates', Icon: WorkflowIcon },
  { to: '/prompts', label: 'Prompt 库', Icon: LibraryIcon },
] as const

const MORE = [
  { to: '/backup', label: '数据备份' },
  { to: '/hosts', label: 'GPU 主机' },
  { to: '/maintenance', label: '维护' },
] as const

const tabCls = (active: boolean) =>
  cn(
    'flex flex-col items-center gap-0.5 py-2 text-xs',
    active ? 'text-primary' : 'text-muted-foreground',
  )

/** 小屏底部导航:三个主入口 + 「更多」上弹菜单;pb-safe 避开 iPhone Home Indicator */
export function MobileTabBar() {
  const { pathname } = useLocation()
  const moreActive = MORE.some((m) => pathname.startsWith(m.to))
  return (
    <nav className="fixed inset-x-0 bottom-0 z-40 border-t bg-background pb-[env(safe-area-inset-bottom)] md:hidden">
      <div className="grid grid-cols-4">
        {TABS.map(({ to, label, Icon }) => (
          <NavLink key={to} to={to} className={({ isActive }) => tabCls(isActive)}>
            <Icon className="size-5" />
            {label}
          </NavLink>
        ))}
        <DropdownMenu>
          <DropdownMenuTrigger className={tabCls(moreActive)}>
            <EllipsisIcon className="size-5" />
            更多
          </DropdownMenuTrigger>
          <DropdownMenuContent side="top" align="end">
            {MORE.map((m) => (
              <DropdownMenuItem key={m.to} asChild>
                <Link to={m.to} className={pathname.startsWith(m.to) ? 'text-primary' : undefined}>
                  {m.label}
                </Link>
              </DropdownMenuItem>
            ))}
          </DropdownMenuContent>
        </DropdownMenu>
      </div>
    </nav>
  )
}
```

注：`DropdownMenuItem` 已在 `dropdown-menu.tsx` 定义并导出（60 行），直接导入即可。

- [ ] **Step 2: 改造 App.tsx 的 RequireToken**

把现有返回块替换为（导航链接收进 `hidden md:flex` 容器，标题小屏可截断，内容区加底部让位）：

```tsx
return (
  <div className="mx-auto max-w-6xl p-4 pb-20 md:p-6 md:pb-6">
    <nav className="mb-4 flex items-center gap-3 border-b pb-3 md:mb-6 md:gap-6 md:pb-4">
      <span className="min-w-0 truncate font-semibold">Comfy Workflow Executor</span>
      <div className="hidden items-center gap-6 md:flex">
        <NavLink to="/batches" className={navCls}>
          Batches
        </NavLink>
        <NavLink to="/templates" className={navCls}>
          Templates
        </NavLink>
        <NavLink to="/prompts" className={navCls}>
          Prompt 库
        </NavLink>
        <NavLink to="/backup" className={navCls}>
          数据备份
        </NavLink>
        <NavLink to="/hosts" className={navCls}>
          GPU 主机
        </NavLink>
        <NavLink to="/maintenance" className={navCls}>
          维护
        </NavLink>
      </div>
      <ThemeToggle />
      <HostStatus />
    </nav>
    <Outlet />
    <MobileTabBar />
    <Toaster richColors position="bottom-right" theme={resolved} mobileOffset={{ bottom: 80 }} />
  </div>
)
```

顶部加导入：`import { MobileTabBar } from '@/components/mobile-tab-bar'`。
`ThemeToggle` 内部按钮已有 `ml-auto`，两种断点下都把主题/主机状态推到右侧，不用动。
`mobileOffset` 是 sonner v2 自带 prop（本项目 `sonner@^2.0.7`），小屏 toast 上移避开 Tab 栏。

- [ ] **Step 3: 验证**

Run: `pnpm --filter @cwe/web typecheck && pnpm --filter @cwe/web test`
Expected: 通过。再 `pnpm --filter @cwe/web dev` 后浏览器 DevTools iPhone SE（375px）检查：底部 Tab 栏可见且当前页高亮、「更多」向上弹出并可跳转、桌面宽度（≥768px）下 Tab 栏消失且顶栏与改动前一致。

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/components/mobile-tab-bar.tsx apps/web/src/App.tsx
git commit -m "feat(web): 小屏底部 Tab 栏,顶栏精简为标题+状态+主题

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: DataTable renderCard + Batches 卡片

**Files:**
- Modify: `apps/web/src/components/data-table/data-table.tsx`（props 接口约 96-106 行、组件主体约 108-223 行）
- Modify: `apps/web/src/pages/batches.tsx`

**Interfaces:**
- Consumes: Task 1 的 `useMediaQuery`。
- Produces: `DataTable` 新 prop `renderCard?: (row: Row<TData>) => React.ReactNode`。

- [ ] **Step 1: data-table.tsx 加 renderCard**

导入区加：

```ts
import { useMediaQuery } from '@/hooks/use-media-query'
```

`@tanstack/react-table` 的 type 导入里补 `type Row`。

`DataTableProps` 加字段（放 `reorder` 之前）：

```ts
/** 提供时,小屏(<md)下用卡片列表替换表格主体;筛选/排序/选择/分页照常生效 */
renderCard?: (row: Row<TData>) => React.ReactNode
```

组件参数解构加 `renderCard`。`const rows = table.getRowModel().rows` 之后加：

```tsx
const isMobile = useMediaQuery('(max-width: 767px)')
const asCards = renderCard !== undefined && isMobile
```

return 中把 `{reorder ? (...) : tableEl}` 整段换成：

```tsx
{asCards ? (
  rows.length === 0 ? (
    <p className="py-8 text-center text-sm text-muted-foreground">{emptyText}</p>
  ) : (
    <div className="space-y-2">
      {rows.map((row) => (
        <React.Fragment key={row.id}>{renderCard!(row)}</React.Fragment>
      ))}
    </div>
  )
) : reorder ? (
  <DndDisabledContext.Provider value={dndDisabled}>
    <DndContext
      sensors={sensors}
      collisionDetection={closestCenter}
      modifiers={[restrictToVerticalAxis]}
      onDragEnd={onDragEnd}
    >
      <SortableContext items={rows.map((r) => r.id)} strategy={verticalListSortingStrategy}>
        {tableEl}
      </SortableContext>
    </DndContext>
  </DndDisabledContext.Provider>
) : (
  tableEl
)}
```

（`ui/table.tsx` 的 Table 已自带 `overflow-x-auto` 容器，未传 `renderCard` 的表格页小屏横滑天然可用，无需额外改动。）

- [ ] **Step 2: batches.tsx 加 BatchCard 并接线**

导入区加：

```ts
import type { Row } from '@tanstack/react-table'
import { Progress } from '@/components/ui/progress'
import { cn, formatUtcDateTime } from '@/lib/utils'
```

（`formatUtcDateTime` 原有，合并进同一行导入。）

文件内（`BatchFilters` 之前）加组件：

```tsx
/** 小屏卡片:整卡 Link 覆盖跳详情,Checkbox 提到 z-10 之上避免触发跳转 */
function BatchCard({ row }: { row: Row<BatchSummaryDto> }) {
  const b = row.original
  const done = b.succeeded + b.failed
  return (
    <div className={cn('relative rounded-md border p-3', row.getIsSelected() && 'border-primary')}>
      <Link to={`/batches/${b.id}`} className="absolute inset-0" aria-label={b.name} />
      <div className="flex items-center gap-2">
        <span className="relative z-10 flex items-center">
          <Checkbox
            checked={row.getIsSelected()}
            onCheckedChange={(v) => row.toggleSelected(!!v)}
            aria-label="选择"
          />
        </span>
        <span className="min-w-0 flex-1 truncate font-medium">{b.name}</span>
        <StatusBadge status={b.status} />
      </div>
      <div className="mt-2 flex items-center gap-2">
        <Progress className="flex-1" value={(done / Math.max(b.total, 1)) * 100} />
        <span className="text-xs whitespace-nowrap">
          {done}/{b.total}
          {b.failed > 0 && <span className="ml-1 text-destructive">({b.failed} 失败)</span>}
        </span>
      </div>
      <div className="mt-1 flex items-center justify-between gap-2 text-xs text-muted-foreground">
        <span className="min-w-0 truncate">{b.templateName}</span>
        <span className="whitespace-nowrap">{formatUtcDateTime(b.createdAt)}</span>
      </div>
    </div>
  )
}
```

`BatchesPage` 的 `<DataTable ...>` 加 prop：

```tsx
renderCard={(row) => <BatchCard row={row} />}
```

- [ ] **Step 3: 验证**

Run: `pnpm --filter @cwe/web typecheck && pnpm --filter @cwe/web test`
Expected: 通过。dev server + DevTools 375px 检查 Batches 页：卡片列表渲染、搜索/状态筛选/模板筛选对卡片生效、勾选卡片出现批量操作栏且不跳详情、点卡片其他区域进详情、桌面宽度仍是原表格。

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/components/data-table/data-table.tsx apps/web/src/pages/batches.tsx
git commit -m "feat(web): DataTable renderCard 小屏卡片形态,Batches 接入监控卡片

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: batch-detail 移动适配 + Lightbox 滑动翻页 + 触屏对比

**Files:**
- Modify: `apps/web/src/pages/batch-detail.tsx`
- Modify: `apps/web/src/components/image-compare.tsx`

**Interfaces:**
- Consumes: 均为内部改动，无跨任务接口。
- Produces: 无（对外接口不变）。

- [ ] **Step 1: 网格与头部响应式**

`batch-detail.tsx` 四处：

1. 骨架屏网格（约 103 行）：`grid grid-cols-4 gap-4` → `grid grid-cols-2 gap-4 md:grid-cols-4`
2. 画廊网格（约 225 行）：同上改法。
3. 页头容器（约 129 行）：`flex items-center justify-between` → `flex flex-wrap items-center justify-between gap-2`；其内左侧 `flex items-center gap-3` → `flex min-w-0 flex-wrap items-center gap-x-3 gap-y-1`。
4. 页头右侧操作组（约 153 行）：`<div className="space-x-2">` → `<div className="flex flex-wrap gap-2">`（`space-x` 在换行时首元素缺左距，换 gap）。

- [ ] **Step 2: Lightbox 滑动翻页**

`batch-detail.tsx` 顶部 `useState` 导入行补 `useRef`。`Lightbox` 组件内（`useEffect` 之前）加：

```tsx
// 非对比模式的横向快滑翻页;对比模式下由 ImageCompare 接管指针,天然互斥
const swipeStart = useRef<{ x: number; y: number } | null>(null)
```

非对比分支的 `<img>` 替换为：

```tsx
<img
  src={outputUrl(cur.output.path)}
  alt={cur.output.filename}
  draggable={false}
  className="max-h-[70vh] w-full touch-pan-y rounded-md object-contain select-none"
  onPointerDown={(e) => {
    swipeStart.current = { x: e.clientX, y: e.clientY }
  }}
  onPointerUp={(e) => {
    const s = swipeStart.current
    swipeStart.current = null
    if (!s) return
    const dx = e.clientX - s.x
    const dy = e.clientY - s.y
    if (Math.abs(dx) < 48 || Math.abs(dx) <= Math.abs(dy)) return
    if (dx > 0 && index > 0) onIndex(index - 1)
    if (dx < 0 && index < items.length - 1) onIndex(index + 1)
  }}
/>
```

（`touch-pan-y`：横向手势交给 JS、纵向仍可滚动弹层；48px 阈值过滤点击与斜向滚动。）

- [ ] **Step 3: Lightbox footer 允许换行**

`DialogFooter` 内两个 `<span className="flex gap-2">` 均改为 `flex flex-wrap gap-2`；多参数切换行 `<span className="flex gap-1">`（约 342 行）改为 `flex flex-wrap gap-1`。

- [ ] **Step 4: ImageCompare 触屏支持**

`image-compare.tsx` 两处：

1. 交互容器 className：`relative cursor-crosshair select-none` → `relative cursor-crosshair touch-none select-none`（`touch-action: none` 仅此容器，把触摸拖动完整交给 pointermove，不影响弹层其他区域滚动）。
2. `onPointerLeave` 改为仅鼠标回弹，触摸松手停留在当前位置：

```tsx
onPointerLeave={(e) => {
  if (e.pointerType === 'mouse') setPos(50)
}}
```

3. 组件头注释第一行后补一句：`触屏:按住拖动跟随,松手停留在当前位置(不回弹)。`

- [ ] **Step 5: 验证**

Run: `pnpm --filter @cwe/web typecheck && pnpm --filter @cwe/web test`
Expected: 通过。dev server 检查：375px 下画廊两列、页头按钮换行不溢出；Lightbox 中触摸（DevTools touch 模拟）横滑切图、纵滑仍滚动、点击不切图；开对比后拖动分割线跟随、松手停留、横滑不再翻页；桌面鼠标悬停跟随 + 移出回弹 50% 无回归。

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/pages/batch-detail.tsx apps/web/src/components/image-compare.tsx
git commit -m "feat(web): batch-detail 移动适配——两列画廊/滑动翻页/触屏对比拖动

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: 其余页面兜底

**Files:**
- Modify: `apps/web/src/pages/login.tsx`（约 27 行）
- Modify: `apps/web/src/pages/batch-new.tsx`（约 514 行）
- Modify: `apps/web/src/pages/template-import.tsx`（约 408、416 行）

**Interfaces:** 无。

- [ ] **Step 1: 三处精确修改**

1. `login.tsx`：`<Card className="w-96">` → `<Card className="w-full max-w-96">`（`w-96`=384px 超过 375px 视口）。
2. `batch-new.tsx`：`<div className="grid grid-cols-2 gap-4">` → `<div className="grid grid-cols-1 gap-4 md:grid-cols-2">`。
3. `template-import.tsx` 节点映射行：外层 `<div className="divide-y border-t">` → `<div className="divide-y overflow-x-auto border-t">`；行容器 `flex items-center gap-3 px-4 py-2` → `flex min-w-fit items-center gap-3 px-4 py-2`（固定宽 `w-40`/`w-56` 的行在窄屏改为横滑而非挤压）。

其余固定宽（`w-64`/`w-72`/`w-56` 等 ≤288px）在 375px 视口减去 `p-4` 后（343px）均不溢出，不动；`ui/table.tsx` 已带 `overflow-x-auto`，所有表格页天然横滑。

- [ ] **Step 2: 验证**

Run: `pnpm --filter @cwe/web typecheck && pnpm --filter @cwe/web test`
Expected: 通过。dev server 375px 逐页过一遍（login / batches / batch-new / batch-detail / templates / templates/new / prompts / backup / hosts / maintenance）：`document.documentElement.scrollWidth <= 375`（DevTools console 验证无横向溢出），控件可点。发现新溢出点按同样思路（收窄/换行/横滑）就地修复并记录进 commit message。

- [ ] **Step 3: Commit**

```bash
git add apps/web/src/pages/login.tsx apps/web/src/pages/batch-new.tsx apps/web/src/pages/template-import.tsx
git commit -m "fix(web): 小屏兜底——login 卡片收窄/batch-new 单列/导入映射行横滑

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

（若 Step 2 有额外修复，`git add` 相应文件并在 message 中列出。）

---

### Task 6: PWA 轻量外壳

**Files:**
- Create: `apps/web/public/manifest.webmanifest`
- Create: `apps/web/public/icon.svg`、`icon-192.png`、`icon-512.png`、`apple-touch-icon.png`
- Modify: `apps/web/index.html`

**Interfaces:** 无。

- [ ] **Step 1: 图标源文件**

`apps/web/public/icon.svg`（indigo 主题色 + 双层画框隐喻批量出图/对比）：

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <rect width="512" height="512" rx="96" fill="#4f46e5"/>
  <rect x="96" y="96" width="200" height="200" rx="24" fill="#ffffff" opacity="0.92"/>
  <rect x="216" y="216" width="200" height="200" rx="24" fill="#ffffff" opacity="0.55"/>
</svg>
```

- [ ] **Step 2: 生成 PNG（macOS 本机）**

```bash
cd apps/web/public
qlmanage -t -s 512 icon.svg -o . && mv icon.svg.png icon-512.png
sips -z 192 192 icon-512.png --out icon-192.png
sips -z 180 180 icon-512.png --out apple-touch-icon.png
```

验证：`sips -g pixelWidth icon-512.png` 输出 512；用 Read 工具打开 `icon-512.png` 目检渲染正常（圆角 indigo 底 + 白色双框）。若 `qlmanage` 渲染异常，改用 `rsvg-convert -w 512 icon.svg -o icon-512.png`（`brew install librsvg`）。

- [ ] **Step 3: manifest**

`apps/web/public/manifest.webmanifest`：

```json
{
  "name": "Comfy Workflow Executor",
  "short_name": "CWE",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#f8fafc",
  "theme_color": "#f8fafc",
  "icons": [
    { "src": "/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

（`#f8fafc` ≈ 亮色 `--background` oklch(0.985 0.002 247.839)；暗色由 index.html 的 `theme-color` media 变体覆盖，manifest 只能单值。）

- [ ] **Step 4: index.html**

viewport 行改为：

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
```

`<title>` 之前插入：

```html
<meta name="theme-color" media="(prefers-color-scheme: light)" content="#f8fafc" />
<meta name="theme-color" media="(prefers-color-scheme: dark)" content="#020617" />
<link rel="manifest" href="/manifest.webmanifest" />
<link rel="apple-touch-icon" href="/apple-touch-icon.png" />
```

（`#020617` ≈ 暗色 `--background` slate-950。）

- [ ] **Step 5: 验证**

Run: `pnpm --filter @cwe/web build`
Expected: 构建成功且 `apps/web/dist/` 根含 `manifest.webmanifest`、三个 PNG（vite 原样拷贝 `public/`）。dev server 下 DevTools → Application → Manifest 无告警、图标可见。

- [ ] **Step 6: Commit**

```bash
git add apps/web/public apps/web/index.html
git commit -m "feat(web): PWA 轻量外壳——manifest/图标/theme-color/safe-area viewport

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: 全量验证

**Files:** 无新改动（只跑验证）。

- [ ] **Step 1: 全仓测试与类型检查**

Run: `pnpm test && pnpm typecheck`
Expected: shared 30 / web 32（29+3）/ server 249 全绿；typecheck 3/3 通过。

- [ ] **Step 2: 桌面无回归目检**

dev server ≥1024px 宽逐页快速过一遍：顶栏导航、Batches 表格、batch-detail 画廊四列、Lightbox 悬停对比回弹——应与 main 无视觉差异。

- [ ] **Step 3: 手动验收清单（写入 PR 描述）**

- [ ] 375px：底部 Tab 栏四入口、当前页高亮、safe-area 不被 Home Indicator 遮挡（真机）
- [ ] 「更多」上弹菜单三项可跳转，处于备份/主机/维护时「更多」高亮
- [ ] Batches 卡片：搜索/筛选/批量选择生效；勾选不跳详情、点卡片跳详情
- [ ] batch-detail：画廊两列；页头按钮换行不溢出
- [ ] Lightbox：触屏横滑翻页、纵滑滚动、点击不翻页
- [ ] 对比模式：触屏拖动分割线跟随、松手停留；开启时横滑不翻页
- [ ] 桌面：悬停跟随 + 移出回弹 50% 无回归；≥768px 全部页面与 main 无视觉差异
- [ ] 各页 375px 无横向溢出（DevTools `scrollWidth` 检查）
- [ ] iOS Safari「添加到主屏幕」：图标正确、standalone 无地址栏、切后台回来数据自动刷新
- [ ] 明暗主题下 Tab 栏、卡片、theme-color 状态栏颜色正常

## 执行后流程

Task 7 通过后：superpowers:requesting-code-review 全分支子代理评审 → 修复 → 推送 `feat/mobile-ui` 并以 YmlyZA 账号开 PR（描述含上方验收清单，结尾 🤖 Generated with [Claude Code](https://claude.com/claude-code)），随后切回 thunderbird-ns。用户手动验收后合并。
